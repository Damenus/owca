nameSuffix: -small
bases:
- ../base

commonLabels:
  app: mysql-small
  size: small

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=25
      - count_ware=25
      - worker_building_schema=25

patchesStrategicMerge:
  - resources.yaml
