nameSuffix: -big
bases:
- ../base

commonLabels:
  app: mysql-big
  size: big

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=150
      - count_ware=150
      - worker_building_schema=150

patchesStrategicMerge:
  - resources.yaml
