nameSuffix: -big
bases:
- ../base

commonLabels:
  app: hammerdb-big
  size: big

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=10

patchesStrategicMerge:
  - resources.yaml
