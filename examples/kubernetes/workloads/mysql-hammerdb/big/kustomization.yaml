nameSuffix: -big
bases:
- ../base

commonLabels:
  app: mysql-big
  size: big

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=10

patchesStrategicMerge:
  - resources.yaml
