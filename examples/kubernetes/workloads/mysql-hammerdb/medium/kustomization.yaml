nameSuffix: -medium
bases:
- ../base

commonLabels:
  app: mysql-medium
  size: medium

configMapGenerator:
  - name: hammerdb
    behavior: merge
    literals:
      - number_runners=2

patchesStrategicMerge:
  - resources.yaml
