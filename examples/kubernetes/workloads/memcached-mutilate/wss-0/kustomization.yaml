nameSuffix: -wss-0
bases:
- ../base

commonLabels:
  app: memcached-mutilate-wss-0

configMapGenerator:
- name: mutilate
  behavior: merge
  literals:
  - load_records=1400000
  - records=1400000 #1000000
  - valuesize=100000 # B   # 360000 # 400000 # 80000    # 80kB * 1M  <= 100GB
  - qps=5000
  - threads=4
  - connections=4
#  - extra=--update=0.25

patchesStrategicMerge:
  - resources.yaml