resources:
  - hammerdb.yaml
  - mysql.yaml

configMapGenerator:
  - name: mysql-tpm-gauge-tcl-script
    files:
      - workload.tcl
      - build_workload.tcl

  - name: hammerdb-parameters
    literals:
      - number_runners=1