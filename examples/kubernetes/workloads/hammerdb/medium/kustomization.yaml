nameSuffix: -medium
bases:
- ../base

commonLabels:
  app: hammerdb-medium
  size: medium

configMapGenerator:
  - name: hammerdb-parameters
    behavior: merge
    literals:
      - number_runners=2

patchesStrategicMerge:
  - resources.yaml
