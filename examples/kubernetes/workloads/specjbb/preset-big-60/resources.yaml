apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: specjbb-group
spec:
  template:
    spec:
      containers:
        - name: backend
          resources:
            requests:
              cpu: 16
              memory: 60G
        - name: injector
          resources:
            requests:
              cpu: 1
              memory: 2G
